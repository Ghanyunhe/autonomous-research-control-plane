from __future__ import annotations

from elp_atlas.schemas import CandidateTask


def score_candidate_task(candidate: CandidateTask) -> float:
    """Placeholder seam for cheap ELP scoring."""
    raise NotImplementedError("Cheap ELP scoring is introduced in a later phase.")
