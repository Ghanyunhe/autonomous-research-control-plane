from __future__ import annotations

from elp_atlas.schemas import CandidateTask


def filter_candidates(candidates: list[CandidateTask]) -> list[CandidateTask]:
    """Placeholder seam for candidate filtering."""
    raise NotImplementedError("Candidate filtering is introduced in a later phase.")
