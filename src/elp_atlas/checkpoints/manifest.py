from __future__ import annotations

from elp_atlas.schemas import RoundCheckpoint


def build_checkpoint_manifest() -> list[RoundCheckpoint]:
    """Placeholder seam for checkpoint manifest generation."""
    raise NotImplementedError("Checkpoint manifests are introduced in a later phase.")
